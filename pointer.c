#include <stdio.h>
#include <string.h>

int myFunc(int *num, int *sumNum)
{
    (*num) + *sumNum;
}

int main()
{
    // int *myVar, *myVarTwo;
    // printf("%d\n", myVar);
    // printf("%d", myVarTwo);

    // int numOne = 10;
    // int *pointNum = &numOne;
    // printf("%d", *pointNum);

    // int num = 20;
    // int *numOne = &num;
    // int **numTwo = &numOne;

    // int myNum = 10;
    // int *pointNum = &myNum;
    // int *addingNum = &myNum;

    // myFunc(pointNum, addingNum);
    // printf("%d", myNum);
    return 0;
}